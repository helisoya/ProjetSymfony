{% extends 'base.html.twig' %}

{% block body %}
<div class="example-wrapper">
    <h1>Users : </h1>
    
    <ul>
        {% for user in users %}
            <li>{{ user.name|capitalize }}</li>
        {% endfor %}
    </ul>
</div>
{% endblock %}
